/**
 * ToolMind — Shared Type Definitions
 *
 * Add application-wide TypeScript types and interfaces here.
 * Re-export domain-specific types from sub-modules as the project grows.
 */

export type {};
